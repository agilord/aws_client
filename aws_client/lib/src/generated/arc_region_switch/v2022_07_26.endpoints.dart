// AUTO-GENERATED: This file is generated by the `generator` package.
//
// The AWS endpoint resolver for this service, compiled from its
// `smithy.rules#endpointRuleSet` trait.

// ignore_for_file: unnecessary_null_comparison, unnecessary_string_interpolations

import '../../shared/src/protocol/endpoints/endpoint_functions.dart' as fn;
import '../../shared/src/protocol/endpoints/endpoint_resolver.dart';

ResolvedEndpoint resolveEndpoint({
  String? region,
  String? endpoint,
  bool useFips = false,
  bool useDualStack = false,
  bool? useControlPlaneEndpoint,
}) {
  {
    final partitionResult = fn.awsPartition(region);
    if ((useControlPlaneEndpoint != null) &&
        (useControlPlaneEndpoint == true) &&
        (region != null) &&
        (!(useFips == true)) &&
        (!(endpoint != null)) &&
        (partitionResult.name == 'aws-cn')) {
      return ResolvedEndpoint(
          url:
              'https://arc-region-switch-control-plane.cn-north-1.${partitionResult.dualStackDnsSuffix}',
          authSchemes: [
            EndpointAuthScheme(
                name: 'sigv4',
                signingName: 'arc-region-switch',
                signingRegion: 'cn-north-1')
          ]);
    }
  }
  {
    final partitionResult2 = fn.awsPartition(region);
    if ((!(endpoint != null)) &&
        (useControlPlaneEndpoint != null) &&
        (useControlPlaneEndpoint == true) &&
        (region != null) &&
        (useFips == true)) {
      if (partitionResult2.name == 'aws-cn') {
        throw EndpointResolutionException(
            'Invalid Configuration: FIPS is not supported in this partition');
      }
      return ResolvedEndpoint(
          url:
              'https://arc-region-switch-control-plane-fips.${partitionResult2.implicitGlobalRegion}.${partitionResult2.dualStackDnsSuffix}',
          authSchemes: [
            EndpointAuthScheme(
                name: 'sigv4',
                signingName: 'arc-region-switch',
                signingRegion: partitionResult2.implicitGlobalRegion)
          ]);
    }
  }
  {
    final partitionResult3 = fn.awsPartition(region);
    if ((useControlPlaneEndpoint != null) &&
        (useControlPlaneEndpoint == true) &&
        (region != null) &&
        (!(useFips == true)) &&
        (!(endpoint != null))) {
      return ResolvedEndpoint(
          url:
              'https://arc-region-switch-control-plane.${partitionResult3.implicitGlobalRegion}.${partitionResult3.dualStackDnsSuffix}',
          authSchemes: [
            EndpointAuthScheme(
                name: 'sigv4',
                signingName: 'arc-region-switch',
                signingRegion: partitionResult3.implicitGlobalRegion)
          ]);
    }
  }
  if (endpoint != null) {
    if (useFips == true) {
      throw EndpointResolutionException(
          'Invalid Configuration: FIPS and custom endpoint are not supported');
    }
    return ResolvedEndpoint(url: endpoint);
  }
  if (region != null) {
    {
      final partitionResult4 = fn.awsPartition(region);
      if (useFips == true) {
        return ResolvedEndpoint(
            url:
                'https://arc-region-switch-fips.$region.${partitionResult4.dualStackDnsSuffix}');
      }
      return ResolvedEndpoint(
          url:
              'https://arc-region-switch.$region.${partitionResult4.dualStackDnsSuffix}');
    }
  }
  throw EndpointResolutionException('Invalid Configuration: Missing Region');
}
