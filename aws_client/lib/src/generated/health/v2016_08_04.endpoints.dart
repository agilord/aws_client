// AUTO-GENERATED: This file is generated by the `generator` package.
//
// The AWS endpoint resolver for this service, compiled from its
// `smithy.rules#endpointRuleSet` trait.

// ignore_for_file: unnecessary_null_comparison, unnecessary_string_interpolations

import '../../shared/src/protocol/endpoints/endpoint_functions.dart' as fn;
import '../../shared/src/protocol/endpoints/endpoint_resolver.dart';

ResolvedEndpoint resolveEndpoint({
  String? region,
  String? endpoint,
  bool useFips = false,
  bool useDualStack = false,
}) {
  {
    final partitionResult = fn.awsPartition(region);
    if ((!(endpoint != null)) &&
        (useDualStack == false) &&
        (region != null) &&
        (!(partitionResult.name == 'aws')) &&
        (!(partitionResult.name == 'aws-cn')) &&
        (!(partitionResult.name == 'aws-us-gov')) &&
        (!(partitionResult.name == 'aws-iso')) &&
        (!(partitionResult.name == 'aws-iso-b')) &&
        (!(partitionResult.name == 'aws-iso-e')) &&
        (!(partitionResult.name == 'aws-iso-f'))) {
      if (useFips == true) {
        return ResolvedEndpoint(
            url:
                'https://health-fips.$region.${partitionResult.dualStackDnsSuffix}');
      }
      return ResolvedEndpoint(
          url: 'https://health.$region.${partitionResult.dualStackDnsSuffix}');
    }
  }
  if (endpoint != null) {
    if (useFips == true) {
      throw EndpointResolutionException(
          'Invalid Configuration: FIPS and custom endpoint are not supported');
    }
    if (useDualStack == true) {
      throw EndpointResolutionException(
          'Invalid Configuration: Dualstack and custom endpoint are not supported');
    }
    return ResolvedEndpoint(url: endpoint);
  }
  if (region != null) {
    {
      final partitionResult2 = fn.awsPartition(region);
      if ((useFips == true) && (useDualStack == true)) {
        if ((true == partitionResult2.supportsFips) &&
            (true == partitionResult2.supportsDualStack)) {
          return ResolvedEndpoint(
              url:
                  'https://health-fips.$region.${partitionResult2.dualStackDnsSuffix}');
        }
        throw EndpointResolutionException(
            'FIPS and DualStack are enabled, but this partition does not support one or both');
      }
      if (useFips == true) {
        if (partitionResult2.supportsFips == true) {
          return ResolvedEndpoint(
              url: 'https://health-fips.$region.${partitionResult2.dnsSuffix}');
        }
        throw EndpointResolutionException(
            'FIPS is enabled but this partition does not support FIPS');
      }
      if (useDualStack == true) {
        if (true == partitionResult2.supportsDualStack) {
          return ResolvedEndpoint(
              url:
                  'https://health.$region.${partitionResult2.dualStackDnsSuffix}');
        }
        throw EndpointResolutionException(
            'DualStack is enabled but this partition does not support DualStack');
      }
      if (region == 'aws-global') {
        return ResolvedEndpoint(
            url: 'https://global.health.amazonaws.com',
            authSchemes: [
              EndpointAuthScheme(
                  name: 'sigv4',
                  signingName: 'health',
                  signingRegion: 'us-east-1')
            ]);
      }
      if (region == 'aws-cn-global') {
        return ResolvedEndpoint(
            url: 'https://global.health.amazonaws.com.cn',
            authSchemes: [
              EndpointAuthScheme(
                  name: 'sigv4',
                  signingName: 'health',
                  signingRegion: 'cn-northwest-1')
            ]);
      }
      return ResolvedEndpoint(
          url: 'https://health.$region.${partitionResult2.dnsSuffix}');
    }
  }
  throw EndpointResolutionException('Invalid Configuration: Missing Region');
}
