// AUTO-GENERATED: This file is generated by the `generator` package.
//
// Endpoint resolution conformance tests, derived from the service's
// `smithy.rules#endpointTests` trait.

// ignore_for_file: prefer_single_quotes, prefer_collection_literals, unnecessary_lambdas, unused_import

import 'package:aws_client/src/generated/s3/v2006_03_01.endpoints.dart';
import 'package:aws_client/src/shared/src/protocol/endpoints/endpoint_resolver.dart';
import 'package:test/test.dart';

void main() {
  test('region is not a valid DNS-suffix [0]', () {
    expect(
      () => resolveEndpoint(
          region: 'a b',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region: region was not a valid DNS name.')),
    );
  });
  test('Invalid access point ARN: Not S3 [1]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket:
              'arn:aws:not-s3:us-west-2:123456789012:accesspoint:myendpoint'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The ARN was not for the S3 service, found: not-s3')),
    );
  });
  test('Invalid access point ARN: invalid resource [2]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket:
              'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint:more-data'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The ARN may only contain a single resource component after `accesspoint`.')),
    );
  });
  test('Invalid access point ARN: invalid no ap name [3]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: Expected a resource of the format `accesspoint:<accesspoint name>` but no name was provided')),
    );
  });
  test('Invalid access point ARN: AccountId is invalid [4]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket: 'arn:aws:s3:us-west-2:123456_789012:accesspoint:apname'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The account id may only contain a-z, A-Z, 0-9 and `-`. Found: `123456_789012`')),
    );
  });
  test('Invalid access point ARN: access point name is invalid [5]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:ap_name'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The access point name may only contain a-z, A-Z, 0-9 and `-`. Found: `ap_name`')),
    );
  });
  test('Access points (disable access points explicitly false) [6]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        disableAccessPoints: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint');
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Access points: partition does not support FIPS [7]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          bucket: 'arn:aws:s3:cn-north-1:123456789012:accesspoint:myendpoint'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('Bucket region is invalid [8]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          disableAccessPoints: false,
          bucket: 'arn:aws:s3:us-west -2:123456789012:accesspoint:myendpoint'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region in ARN: `us-west -2` (invalid DNS name)')),
    );
  });
  test(
      'Access points when Access points explicitly disabled (used for CreateBucket) [9]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          disableAccessPoints: true,
          bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Access points are not supported for this operation')),
    );
  });
  test('missing arn type [10]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          disableAccessPoints: true,
          bucket: 'arn:aws:s3:us-west-2:123456789012:'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: `arn:aws:s3:us-west-2:123456789012:` was not a valid ARN')),
    );
  });
  test('SDK::Host + access point + Dualstack is an error [11]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          endpoint: 'https://beta.example.com',
          region: 'cn-north-1',
          useDualStack: true,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('Access point ARN with FIPS & Dualstack [12]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableAccessPoints: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint');
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint-fips.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Access point ARN with Dualstack [13]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableAccessPoints: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint');
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla MRAP [14]', () {
    final resolved = resolveEndpoint(
        bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
        region: 'us-east-1',
        disableMultiRegionAccessPoints: false,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://mfzwi23gnjvgw.mrap.accesspoint.s3-global.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('MRAP does not support FIPS [15]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          region: 'us-east-1',
          disableMultiRegionAccessPoints: false,
          useFips: true,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'S3 MRAP does not support FIPS')),
    );
  });
  test('MRAP does not support DualStack [16]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          region: 'us-east-1',
          disableMultiRegionAccessPoints: false,
          useFips: false,
          useDualStack: true,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'S3 MRAP does not support dual-stack')),
    );
  });
  test('MRAP does not support S3 Accelerate [17]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          region: 'us-east-1',
          disableMultiRegionAccessPoints: false,
          useFips: false,
          useDualStack: false,
          accelerate: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 MRAP does not support S3 Accelerate')),
    );
  });
  test('MRAP explicitly disabled [18]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          region: 'us-east-1',
          disableMultiRegionAccessPoints: true,
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: Multi-Region Access Point ARNs are disabled.')),
    );
  });
  test('Dual-stack endpoint with path-style forced [19]', () {
    final resolved = resolveEndpoint(
        bucket: 'bucketname',
        region: 'us-west-2',
        forcePathStyle: true,
        useFips: false,
        accelerate: false,
        useDualStack: true);
    expect(resolved.url,
        'https://s3.dualstack.us-west-2.amazonaws.com/bucketname');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Dual-stack endpoint + SDK::Host is error [20]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'bucketname',
          region: 'us-west-2',
          forcePathStyle: true,
          useFips: false,
          accelerate: false,
          useDualStack: true,
          endpoint: 'https://abc.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('path style + ARN bucket [21]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'arn:aws:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          forcePathStyle: true,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Path-style addressing cannot be used with ARN buckets')),
    );
  });
  test('implicit path style bucket + dualstack [22]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99_ab',
        region: 'us-west-2',
        useDualStack: true,
        useFips: false);
    expect(resolved.url, 'https://s3.dualstack.us-west-2.amazonaws.com/99_ab');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('implicit path style bucket + dualstack [23]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: '99_ab',
          region: 'us-west-2',
          useDualStack: true,
          useFips: false,
          endpoint: 'http://abc.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('don\'t allow URL injections in the bucket [24]', () {
    final resolved = resolveEndpoint(
        bucket: 'example.com#',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/example.com%23');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('URI encode bucket names in the path [25]', () {
    final resolved = resolveEndpoint(
        bucket: 'bucket name',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/bucket%20name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('scheme is respected [26]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99_ab',
        endpoint:
            'http://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'http://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/99_ab');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('scheme is respected (virtual addressing) [27]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucketname',
        endpoint:
            'http://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/foo',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'http://bucketname.control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/foo');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + implicit private link [28]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99_ab',
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/99_ab');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('invalid Endpoint override [29]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucketname',
          endpoint: 'abcde://nota#url',
          region: 'af-south-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Custom endpoint `abcde://nota#url` was not a valid URI')),
    );
  });
  test('using an IPv4 address forces path style [30]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucketname',
        endpoint: 'https://123.123.0.1',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://123.123.0.1/bucketname');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'vanilla access point arn with region mismatch and UseArnRegion=false [31]',
      () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'arn:aws:s3:us-east-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          useArnRegion: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `us-west-2` and UseArnRegion is `false`')),
    );
  });
  test(
      'vanilla access point arn with region mismatch and UseArnRegion unset [32]',
      () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'us-east-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'vanilla access point arn with region mismatch and UseArnRegion=true [33]',
      () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        useArnRegion: true,
        region: 'us-east-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('subdomains are not allowed in virtual buckets [34]', () {
    final resolved =
        resolveEndpoint(bucket: 'bucket.name', region: 'us-east-1');
    expect(resolved.url, 'https://s3.us-east-1.amazonaws.com/bucket.name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('bucket names with 3 characters are allowed in virtual buckets [35]',
      () {
    final resolved = resolveEndpoint(bucket: 'aaa', region: 'us-east-1');
    expect(resolved.url, 'https://aaa.s3.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'bucket names with fewer than 3 characters are not allowed in virtual host [36]',
      () {
    final resolved = resolveEndpoint(bucket: 'aa', region: 'us-east-1');
    expect(resolved.url, 'https://s3.us-east-1.amazonaws.com/aa');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'bucket names with uppercase characters are not allowed in virtual host [37]',
      () {
    final resolved = resolveEndpoint(bucket: 'BucketName', region: 'us-east-1');
    expect(resolved.url, 'https://s3.us-east-1.amazonaws.com/BucketName');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('subdomains are allowed in virtual buckets on http endpoints [38]', () {
    final resolved = resolveEndpoint(
        bucket: 'bucket.name',
        region: 'us-east-1',
        endpoint: 'http://example.com');
    expect(resolved.url, 'http://bucket.name.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('no region set [39]', () {
    expect(
      () => resolveEndpoint(bucket: 'bucket-name'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A region must be set when sending requests to S3.')),
    );
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1 uses the global endpoint [40]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-west-2 uses the regional endpoint [41]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=cn-north-1 uses the regional endpoint [42]',
      () {
    final resolved = resolveEndpoint(
        region: 'cn-north-1',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1, fips=true uses the regional endpoint with fips [43]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: true,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1, dualstack=true uses the regional endpoint with dualstack [44]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url, 'https://s3.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1, dualstack and fips uses the regional endpoint with fips/dualstack [45]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: true,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url, 'https://s3-fips.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1 with custom endpoint, uses custom [46]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        endpoint: 'https://example.com',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-west-2 with custom endpoint, uses custom [47]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        endpoint: 'https://example.com',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'UseGlobalEndpoints=true, region=us-east-1 with accelerate on non bucket case uses the global endpoint and ignores accelerate [48]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: true);
    expect(resolved.url, 'https://s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global region uses the global endpoint [49]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global region with fips uses the regional endpoint [50]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: true,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global region with dualstack uses the regional endpoint [51]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url, 'https://s3.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'aws-global region with fips and dualstack uses the regional endpoint [52]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: true,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url, 'https://s3-fips.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'aws-global region with accelerate on non-bucket case, uses global endpoint and ignores accelerate [53]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: false,
        useDualStack: false,
        accelerate: true);
    expect(resolved.url, 'https://s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global region with custom endpoint, uses custom [54]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        endpoint: 'https://example.com',
        useGlobalEndpoint: false,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing, aws-global region uses the global endpoint [55]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with Prefix, and Key uses the global endpoint. Prefix and Key parameters should not be used in endpoint evaluation. [56]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        prefix: 'prefix',
        key: 'key');
    expect(resolved.url, 'https://bucket-name.s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with Copy Source, and Key uses the global endpoint. Copy Source and Key parameters should not be used in endpoint evaluation. [57]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        copySource: '/copy/source',
        key: 'key');
    expect(resolved.url, 'https://bucket-name.s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with fips uses the regional fips endpoint [58]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: true,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.s3-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with dualstack uses the regional dualstack endpoint [59]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url,
        'https://bucket-name.s3.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with fips/dualstack uses the regional fips/dualstack endpoint [60]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: true,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url,
        'https://bucket-name.s3-fips.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, aws-global region with accelerate uses the global accelerate endpoint [61]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: true);
    expect(resolved.url, 'https://bucket-name.s3-accelerate.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing, aws-global region with custom endpoint [62]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        endpoint: 'https://example.com',
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-east-1 region uses the global endpoint [63]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.s3.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-west-2 region uses the regional endpoint [64]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.s3.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-east-1 region and fips uses the regional fips endpoint [65]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: true,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.s3-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-east-1 region and dualstack uses the regional dualstack endpoint [66]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url,
        'https://bucket-name.s3.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-east-1 region and accelerate uses the global accelerate endpoint [67]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: true);
    expect(resolved.url, 'https://bucket-name.s3-accelerate.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'virtual addressing, UseGlobalEndpoint and us-east-1 region with custom endpoint [68]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        endpoint: 'https://example.com',
        useGlobalEndpoint: true,
        bucket: 'bucket-name',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://bucket-name.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('ForcePathStyle, aws-global region uses the global endpoint [69]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        forcePathStyle: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('ForcePathStyle, aws-global region with fips is invalid [70]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        forcePathStyle: true,
        useFips: true,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, aws-global region with dualstack uses regional dualstack endpoint [71]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket-name',
        forcePathStyle: true,
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url,
        'https://s3.dualstack.us-east-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, aws-global region custom endpoint uses the custom endpoint [72]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        endpoint: 'https://example.com',
        bucket: 'bucket-name',
        forcePathStyle: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://example.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, UseGlobalEndpoint us-east-1 region uses the global endpoint [73]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket-name',
        useGlobalEndpoint: true,
        forcePathStyle: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, UseGlobalEndpoint us-west-2 region uses the regional endpoint [74]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'bucket-name',
        useGlobalEndpoint: true,
        forcePathStyle: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, UseGlobalEndpoint us-east-1 region, dualstack uses the regional dualstack endpoint [75]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket-name',
        useGlobalEndpoint: true,
        forcePathStyle: true,
        useFips: false,
        useDualStack: true,
        accelerate: false);
    expect(resolved.url,
        'https://s3.dualstack.us-east-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ForcePathStyle, UseGlobalEndpoint us-east-1 region custom endpoint uses the custom endpoint [76]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket-name',
        endpoint: 'https://example.com',
        useGlobalEndpoint: true,
        forcePathStyle: true,
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://example.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'ARN with aws-global region and  UseArnRegion uses the regional endpoint [77]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useArnRegion: true,
        useFips: false,
        useDualStack: false,
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op-01234567890123456/accesspoint/reports');
    expect(resolved.url,
        'https://reports-123456789012.op-01234567890123456.s3-outposts.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('cross partition MRAP ARN is an error [78]', () {
    expect(
      () => resolveEndpoint(
          bucket: 'arn:aws-cn:s3::123456789012:accesspoint:mfzwi23gnjvgw.mrap',
          region: 'us-west-1'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Client was configured for partition `aws` but bucket referred to partition `aws-cn`')),
    );
  });
  test('Endpoint override, accesspoint with HTTP, port [79]', () {
    final resolved = resolveEndpoint(
        endpoint: 'http://beta.example.com:1234',
        region: 'us-west-2',
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint');
    expect(
        resolved.url, 'http://myendpoint-123456789012.beta.example.com:1234');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Endpoint override, accesspoint with http, path, query, and port [80]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        endpoint: 'http://beta.example.com:1234/path',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'http://myendpoint-123456789012.beta.example.com:1234/path');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('non-bucket endpoint override with FIPS = error [81]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          endpoint: 'http://beta.example.com:1234/path',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('FIPS + dualstack + custom endpoint [82]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          endpoint: 'http://beta.example.com:1234/path',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('dualstack + custom endpoint [83]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          endpoint: 'http://beta.example.com:1234/path',
          useFips: false,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('custom endpoint without FIPS/dualstack [84]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        endpoint: 'http://beta.example.com:1234/path',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'http://beta.example.com:1234/path');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('s3 object lambda with access points disabled [85]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint:myendpoint',
          disableAccessPoints: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Access points are not supported for this operation')),
    );
  });
  test('non bucket + FIPS [86]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2', useFips: true, useDualStack: false);
    expect(resolved.url, 'https://s3-fips.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('standard non bucket endpoint [87]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2', useFips: false, useDualStack: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('non bucket endpoint with FIPS + Dualstack [88]', () {
    final resolved =
        resolveEndpoint(region: 'us-west-2', useFips: true, useDualStack: true);
    expect(resolved.url, 'https://s3-fips.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('non bucket endpoint with dualstack [89]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2', useFips: false, useDualStack: true);
    expect(resolved.url, 'https://s3.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('use global endpoint + IP address endpoint override [90]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket',
        useFips: false,
        useDualStack: false,
        endpoint: 'http://127.0.0.1',
        useGlobalEndpoint: true);
    expect(resolved.url, 'http://127.0.0.1/bucket');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('non-dns endpoint + global endpoint [91]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        useFips: false,
        useDualStack: false,
        useGlobalEndpoint: true);
    expect(resolved.url, 'https://s3.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('endpoint override + use global endpoint [92]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        useFips: false,
        useDualStack: false,
        useGlobalEndpoint: true,
        endpoint: 'http://foo.com');
    expect(resolved.url, 'http://foo.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('FIPS + dualstack + non-bucket endpoint [93]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        useFips: true,
        useDualStack: true);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('FIPS + dualstack + non-DNS endpoint [94]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        forcePathStyle: true,
        useFips: true,
        useDualStack: true);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('endpoint override + FIPS + dualstack (BUG) [95]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'bucket!',
          forcePathStyle: true,
          useFips: true,
          useDualStack: false,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('endpoint override + non-dns bucket + FIPS (BUG) [96]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'bucket!',
          useFips: true,
          useDualStack: false,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('FIPS + bucket endpoint + force path style [97]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        forcePathStyle: true,
        useFips: true,
        useDualStack: false,
        useGlobalEndpoint: true);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('bucket + FIPS + force path style [98]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket',
        forcePathStyle: true,
        useFips: true,
        useDualStack: true,
        useGlobalEndpoint: true);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('FIPS + dualstack + use global endpoint [99]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket',
        useFips: true,
        useDualStack: true,
        useGlobalEndpoint: true);
    expect(resolved.url,
        'https://bucket.s3-fips.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('URI encoded bucket + use global endpoint [100]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'bucket!',
          useFips: true,
          useDualStack: false,
          useGlobalEndpoint: true,
          endpoint: 'https://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('FIPS + path based endpoint [101]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useGlobalEndpoint: true);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate + dualstack + global endpoint [102]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket',
        useFips: false,
        useDualStack: true,
        accelerate: true,
        useGlobalEndpoint: true);
    expect(
        resolved.url, 'https://bucket.s3-accelerate.dualstack.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('dualstack + global endpoint + non URI safe bucket [103]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        accelerate: false,
        useDualStack: true,
        useFips: false,
        useGlobalEndpoint: true);
    expect(
        resolved.url, 'https://s3.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('FIPS + uri encoded bucket [104]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        forcePathStyle: true,
        accelerate: false,
        useDualStack: false,
        useFips: true,
        useGlobalEndpoint: true);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('endpoint override + non-uri safe endpoint + force path style [105]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'bucket!',
          forcePathStyle: true,
          accelerate: false,
          useDualStack: false,
          useFips: true,
          endpoint: 'http://foo.com',
          useGlobalEndpoint: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('FIPS + Dualstack + global endpoint + non-dns bucket [106]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'bucket!',
        accelerate: false,
        useDualStack: true,
        useFips: true,
        useGlobalEndpoint: true);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('endpoint override + FIPS + dualstack [107]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useDualStack: true,
          useFips: true,
          useGlobalEndpoint: true,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('non-bucket endpoint override + dualstack + global endpoint [108]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: true,
          useGlobalEndpoint: true,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('Endpoint override + UseGlobalEndpoint + us-east-1 [109]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: true,
          useDualStack: false,
          useGlobalEndpoint: true,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('non-FIPS partition with FIPS set + custom endpoint [110]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          useFips: true,
          useDualStack: false,
          useGlobalEndpoint: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('aws-global signs as us-east-1 [111]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket!',
        useFips: true,
        accelerate: false,
        useDualStack: true);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global signs as us-east-1 [112]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket',
        useDualStack: false,
        useFips: false,
        accelerate: false,
        endpoint: 'https://foo.com');
    expect(resolved.url, 'https://bucket.foo.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global + dualstack + path-only bucket [113]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket!',
        useDualStack: true,
        useFips: false,
        accelerate: false);
    expect(
        resolved.url, 'https://s3.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global + path-only bucket [114]', () {
    final resolved = resolveEndpoint(region: 'aws-global', bucket: 'bucket!');
    expect(resolved.url, 'https://s3.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global + fips + custom endpoint [115]', () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          bucket: 'bucket!',
          useDualStack: false,
          useFips: true,
          accelerate: false,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('aws-global, endpoint override & path only-bucket [116]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket!',
        useDualStack: false,
        useFips: false,
        accelerate: false,
        endpoint: 'http://foo.com');
    expect(resolved.url, 'http://foo.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global + dualstack + custom endpoint [117]', () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          useDualStack: true,
          useFips: false,
          accelerate: false,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('accelerate, dualstack + aws-global [118]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket',
        useDualStack: true,
        useFips: false,
        accelerate: true);
    expect(resolved.url,
        'https://bucket.s3-accelerate.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'FIPS + aws-global + path only bucket. This is not supported by S3 but we allow garbage in garbage out [119]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        bucket: 'bucket!',
        forcePathStyle: true,
        useDualStack: true,
        useFips: true,
        accelerate: false);
    expect(resolved.url,
        'https://s3-fips.dualstack.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('aws-global + FIPS + endpoint override. [120]', () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global', useFips: true, endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('force path style, FIPS, aws-global & endpoint override [121]', () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          bucket: 'bucket!',
          forcePathStyle: true,
          useFips: true,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('ip address causes path style to be forced [122]', () {
    final resolved = resolveEndpoint(
        region: 'aws-global', bucket: 'bucket', endpoint: 'http://192.168.1.1');
    expect(resolved.url, 'http://192.168.1.1/bucket');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('endpoint override with aws-global region [123]', () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          useFips: true,
          useDualStack: true,
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('FIPS + path-only (TODO: consider making this an error) [124]', () {
    final resolved =
        resolveEndpoint(region: 'aws-global', bucket: 'bucket!', useFips: true);
    expect(resolved.url, 'https://s3-fips.us-east-1.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('empty arn type [125]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket: 'arn:aws:not-s3:us-west-2:123456789012::myendpoint'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Invalid ARN: No ARN type specified')),
    );
  });
  test('path style can\'t be used with accelerate [126]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2', bucket: 'bucket!', accelerate: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Path-style addressing cannot be used with S3 Accelerate')),
    );
  });
  test('invalid region [127]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2!',
          bucket: 'bucket.subdomain',
          endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region: region was not a valid DNS name.')),
    );
  });
  test('invalid region [128]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2!', bucket: 'bucket', endpoint: 'http://foo.com'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region: region was not a valid DNS name.')),
    );
  });
  test('empty arn type [129]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket: 'arn:aws:s3::123456789012:accesspoint:my_endpoint'),
      throwsA(isA<EndpointResolutionException>()
          .having((e) => e.message, 'message', 'Invalid Access Point Name')),
    );
  });
  test('empty arn type [130]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket: 'arn:aws:s3:cn-north-1:123456789012:accesspoint:my-endpoint',
          useArnRegion: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Client was configured for partition `aws` but ARN (`arn:aws:s3:cn-north-1:123456789012:accesspoint:my-endpoint`) has `aws-cn`')),
    );
  });
  test('invalid arn region [131]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-object-lambda:us-east_2:123456789012:accesspoint:my-endpoint',
          useArnRegion: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region in ARN: `us-east_2` (invalid DNS name)')),
    );
  });
  test('invalid ARN outpost [132]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op_01234567890123456/accesspoint/reports',
          useArnRegion: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The outpost Id may only contain a-z, A-Z, 0-9 and `-`. Found: `op_01234567890123456`')),
    );
  });
  test('invalid ARN [133]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op-01234567890123456/reports'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid ARN: expected an access point name')),
    );
  });
  test('invalid ARN [134]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op-01234567890123456'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid ARN: Expected a 4-component resource')),
    );
  });
  test('invalid outpost type [135]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op-01234567890123456/not-accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Expected an outpost type `accesspoint`, found not-accesspoint')),
    );
  });
  test('invalid outpost type [136]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east_1:123456789012:outpost/op-01234567890123456/not-accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region in ARN: `us-east_1` (invalid DNS name)')),
    );
  });
  test('invalid outpost type [137]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket:
              'arn:aws:s3-outposts:us-east-1:12345_789012:outpost/op-01234567890123456/not-accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The account id may only contain a-z, A-Z, 0-9 and `-`. Found: `12345_789012`')),
    );
  });
  test('invalid outpost type [138]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-2',
          bucket: 'arn:aws:s3-outposts:us-east-1:12345789012:outpost'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid ARN: The Outpost Id was not set')),
    );
  });
  test('use global endpoint virtual addressing [139]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2',
        bucket: 'bucket',
        endpoint: 'http://example.com',
        useGlobalEndpoint: true);
    expect(resolved.url, 'http://bucket.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('global endpoint + ip address [140]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2',
        bucket: 'bucket',
        endpoint: 'http://192.168.0.1',
        useGlobalEndpoint: true);
    expect(resolved.url, 'http://192.168.0.1/bucket');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('invalid outpost type [141]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2', bucket: 'bucket!', useGlobalEndpoint: true);
    expect(resolved.url, 'https://s3.us-east-2.amazonaws.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('invalid outpost type [142]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2',
        bucket: 'bucket',
        accelerate: true,
        useGlobalEndpoint: true);
    expect(resolved.url, 'https://bucket.s3-accelerate.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('use global endpoint + custom endpoint [143]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2',
        bucket: 'bucket!',
        useGlobalEndpoint: true,
        endpoint: 'http://foo.com');
    expect(resolved.url, 'http://foo.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('use global endpoint, not us-east-1, force path style [144]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-2',
        bucket: 'bucket!',
        useGlobalEndpoint: true,
        forcePathStyle: true,
        endpoint: 'http://foo.com');
    expect(resolved.url, 'http://foo.com/bucket%21');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla virtual addressing@us-west-2 [145]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://bucket-name.s3.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + dualstack@us-west-2 [146]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.s3.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate + dualstack@us-west-2 [147]', () {
    final resolved = resolveEndpoint(
        accelerate: true,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.s3-accelerate.dualstack.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate (dualstack=false)@us-west-2 [148]', () {
    final resolved = resolveEndpoint(
        accelerate: true,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://bucket-name.s3-accelerate.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + fips@us-west-2 [149]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: true);
    expect(resolved.url, 'https://bucket-name.s3-fips.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + dualstack + fips@us-west-2 [150]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: true,
        useFips: true);
    expect(resolved.url,
        'https://bucket-name.s3-fips.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate + fips = error@us-west-2 [151]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Accelerate cannot be used with FIPS')),
    );
  });
  test('vanilla virtual addressing@cn-north-1 [152]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://bucket-name.s3.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + dualstack@cn-north-1 [153]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'cn-north-1',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.s3.dualstack.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate (dualstack=false)@cn-north-1 [154]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Accelerate cannot be used in this region')),
    );
  });
  test('virtual addressing + fips@cn-north-1 [155]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('vanilla virtual addressing@af-south-1 [156]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://bucket-name.s3.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + dualstack@af-south-1 [157]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.s3.dualstack.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate + dualstack@af-south-1 [158]', () {
    final resolved = resolveEndpoint(
        accelerate: true,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.s3-accelerate.dualstack.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate (dualstack=false)@af-south-1 [159]', () {
    final resolved = resolveEndpoint(
        accelerate: true,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://bucket-name.s3-accelerate.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + fips@af-south-1 [160]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: false,
        useFips: true);
    expect(
        resolved.url, 'https://bucket-name.s3-fips.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + dualstack + fips@af-south-1 [161]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: true,
        useFips: true);
    expect(resolved.url,
        'https://bucket-name.s3-fips.dualstack.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('accelerate + fips = error@af-south-1 [162]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          region: 'af-south-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Accelerate cannot be used with FIPS')),
    );
  });
  test('vanilla path style@us-west-2 [163]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('fips@us-gov-west-2, bucket is not S3-dns-compatible (subdomains) [164]',
      () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket.with.dots',
        region: 'us-gov-west-1',
        useDualStack: false,
        useFips: true);
    expect(resolved.url,
        'https://s3-fips.us-gov-west-1.amazonaws.com/bucket.with.dots');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-gov-west-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + accelerate = error@us-west-2 [165]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: true,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Path-style addressing cannot be used with S3 Accelerate')),
    );
  });
  test('path style + dualstack@us-west-2 [166]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'us-west-2',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://s3.dualstack.us-west-2.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + arn is error@us-west-2 [167]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:PARTITION:s3-outposts:REGION:123456789012:outpost:op-01234567890123456:bucket:mybucket',
          forcePathStyle: true,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Path-style addressing cannot be used with ARN buckets')),
    );
  });
  test('path style + invalid DNS name@us-west-2 [168]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        forcePathStyle: true,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('no path style + invalid DNS name@us-west-2 [169]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.us-west-2.amazonaws.com/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla path style@cn-north-1 [170]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.cn-north-1.amazonaws.com.cn/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + fips@cn-north-1 [171]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: true,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('path style + accelerate = error@cn-north-1 [172]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: true,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Path-style addressing cannot be used with S3 Accelerate')),
    );
  });
  test('path style + dualstack@cn-north-1 [173]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'cn-north-1',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://s3.dualstack.cn-north-1.amazonaws.com.cn/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + arn is error@cn-north-1 [174]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:PARTITION:s3-outposts:REGION:123456789012:outpost:op-01234567890123456:bucket:mybucket',
          forcePathStyle: true,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Path-style addressing cannot be used with ARN buckets')),
    );
  });
  test('path style + invalid DNS name@cn-north-1 [175]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        forcePathStyle: true,
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.cn-north-1.amazonaws.com.cn/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('no path style + invalid DNS name@cn-north-1 [176]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.cn-north-1.amazonaws.com.cn/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla path style@af-south-1 [177]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.af-south-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + fips@af-south-1 [178]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'af-south-1',
        useDualStack: false,
        useFips: true);
    expect(
        resolved.url, 'https://s3-fips.af-south-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + accelerate = error@af-south-1 [179]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: true,
          region: 'af-south-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Path-style addressing cannot be used with S3 Accelerate')),
    );
  });
  test('path style + dualstack@af-south-1 [180]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        region: 'af-south-1',
        useDualStack: true,
        useFips: false);
    expect(resolved.url,
        'https://s3.dualstack.af-south-1.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + arn is error@af-south-1 [181]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:PARTITION:s3-outposts:REGION:123456789012:outpost:op-01234567890123456:bucket:mybucket',
          forcePathStyle: true,
          region: 'af-south-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Path-style addressing cannot be used with ARN buckets')),
    );
  });
  test('path style + invalid DNS name@af-south-1 [182]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        forcePathStyle: true,
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.af-south-1.amazonaws.com/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('no path style + invalid DNS name@af-south-1 [183]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: '99a_b',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3.af-south-1.amazonaws.com/99a_b');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + private link@us-west-2 [184]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        endpoint:
            'http://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'http://bucket-name.control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + private link@us-west-2 [185]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('SDK::Host + FIPS@us-west-2 [186]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'us-west-2',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('SDK::Host + DualStack@us-west-2 [187]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'us-west-2',
          useDualStack: true,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('SDK::HOST + accelerate@us-west-2 [188]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'http://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'A custom endpoint cannot be combined with S3 Accelerate')),
    );
  });
  test('SDK::Host + access point ARN@us-west-2 [189]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        endpoint: 'https://beta.example.com',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://myendpoint-123456789012.beta.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + private link@cn-north-1 [190]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + private link@cn-north-1 [191]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('FIPS@cn-north-1 [192]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('SDK::Host + DualStack@cn-north-1 [193]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'cn-north-1',
          useDualStack: true,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('SDK::HOST + accelerate@cn-north-1 [194]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'cn-north-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'A custom endpoint cannot be combined with S3 Accelerate')),
    );
  });
  test('SDK::Host + access point ARN@cn-north-1 [195]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        endpoint: 'https://beta.example.com',
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://myendpoint-123456789012.beta.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('virtual addressing + private link@af-south-1 [196]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: false,
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://bucket-name.control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('path style + private link@af-south-1 [197]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'bucket-name',
        forcePathStyle: true,
        endpoint:
            'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com/bucket-name');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('SDK::Host + FIPS@af-south-1 [198]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'af-south-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'A custom endpoint cannot be combined with FIPS')),
    );
  });
  test('SDK::Host + DualStack@af-south-1 [199]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'af-south-1',
          useDualStack: true,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Cannot set dual-stack in combination with a custom endpoint.')),
    );
  });
  test('SDK::HOST + accelerate@af-south-1 [200]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'bucket-name',
          forcePathStyle: false,
          endpoint:
              'https://control.vpce-1a2b3c4d-5e6f.s3.us-west-2.vpce.amazonaws.com',
          region: 'af-south-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'A custom endpoint cannot be combined with S3 Accelerate')),
    );
  });
  test('SDK::Host + access point ARN@af-south-1 [201]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:af-south-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        endpoint: 'https://beta.example.com',
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://myendpoint-123456789012.beta.example.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla access point arn@us-west-2 [202]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('access point arn + FIPS@us-west-2 [203]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: true);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint-fips.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('access point arn + accelerate = error@us-west-2 [204]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Access Points do not support S3 Accelerate')),
    );
  });
  test('access point arn + FIPS + DualStack@us-west-2 [205]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:us-west-2:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: true,
        useFips: true);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint-fips.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('vanilla access point arn@cn-north-1 [206]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'cn-north-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('access point arn + FIPS@cn-north-1 [207]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('access point arn + accelerate = error@cn-north-1 [208]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket:
              'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Access Points do not support S3 Accelerate')),
    );
  });
  test('access point arn + FIPS + DualStack@cn-north-1 [209]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws-cn:s3:cn-north-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          region: 'cn-north-1',
          useDualStack: true,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('vanilla access point arn@af-south-1 [210]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:af-south-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('access point arn + FIPS@af-south-1 [211]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:af-south-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: false,
        useFips: true);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint-fips.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('access point arn + accelerate = error@af-south-1 [212]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          bucket: 'arn:aws:s3:af-south-1:123456789012:accesspoint:myendpoint',
          forcePathStyle: false,
          region: 'af-south-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Access Points do not support S3 Accelerate')),
    );
  });
  test('access point arn + FIPS + DualStack@af-south-1 [213]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket: 'arn:aws:s3:af-south-1:123456789012:accesspoint:myendpoint',
        forcePathStyle: false,
        region: 'af-south-1',
        useDualStack: true,
        useFips: true);
    expect(resolved.url,
        'https://myendpoint-123456789012.s3-accesspoint-fips.dualstack.af-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'af-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 outposts vanilla test [214]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-west-2:123456789012:outpost/op-01234567890123456/accesspoint/reports');
    expect(resolved.url,
        'https://reports-123456789012.op-01234567890123456.s3-outposts.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 outposts custom endpoint [215]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-west-2:123456789012:outpost/op-01234567890123456/accesspoint/reports',
        endpoint: 'https://example.amazonaws.com');
    expect(resolved.url,
        'https://reports-123456789012.op-01234567890123456.example.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('outposts arn with region mismatch and UseArnRegion=false [216]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint',
          forcePathStyle: false,
          useArnRegion: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `us-west-2` and UseArnRegion is `false`')),
    );
  });
  test(
      'outposts arn with region mismatch, custom region and UseArnRegion=false [217]',
      () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-east-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint',
          endpoint: 'https://example.com',
          forcePathStyle: false,
          useArnRegion: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `us-west-2` and UseArnRegion is `false`')),
    );
  });
  test('outposts arn with region mismatch and UseArnRegion=true [218]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-east-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint',
        forcePathStyle: false,
        useArnRegion: true,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myaccesspoint-123456789012.op-01234567890123456.s3-outposts.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('outposts arn with region mismatch and UseArnRegion unset [219]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-east-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint',
        forcePathStyle: false,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url,
        'https://myaccesspoint-123456789012.op-01234567890123456.s3-outposts.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('outposts arn with partition mismatch and UseArnRegion=true [220]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:cn-north-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint',
          forcePathStyle: false,
          useArnRegion: true,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Client was configured for partition `aws` but ARN (`arn:aws:s3-outposts:cn-north-1:123456789012:outpost:op-01234567890123456:accesspoint:myaccesspoint`) has `aws-cn`')),
    );
  });
  test(
      'ARN with UseGlobalEndpoint and use-east-1 region uses the regional endpoint [221]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useGlobalEndpoint: true,
        useFips: false,
        useDualStack: false,
        accelerate: false,
        bucket:
            'arn:aws:s3-outposts:us-east-1:123456789012:outpost/op-01234567890123456/accesspoint/reports');
    expect(resolved.url,
        'https://reports-123456789012.op-01234567890123456.s3-outposts.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 outposts does not support dualstack [222]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: true,
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-west-2:123456789012:outpost/op-01234567890123456/accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Outposts does not support Dual-stack')),
    );
  });
  test('S3 outposts does not support fips [223]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-west-2:123456789012:outpost/op-01234567890123456/accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'S3 Outposts does not support FIPS')),
    );
  });
  test('S3 outposts does not support accelerate [224]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useFips: false,
          useDualStack: false,
          accelerate: true,
          bucket:
              'arn:aws:s3-outposts:us-west-2:123456789012:outpost/op-01234567890123456/accesspoint/reports'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Outposts does not support S3 Accelerate')),
    );
  });
  test('validates against subresource [225]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-west-2:123456789012:outpost:op-01234567890123456:accesspoint:mybucket:object:foo'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Arn: Outpost Access Point ARN contains sub resources')),
    );
  });
  test('validates against access point host label [226]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          bucket:
              'arn:aws:s3-outposts:us-west-2:123456789012:outpost:op-01234567890123456:accesspoint:invalid.bucket#'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The access point name may only contain a-z, A-Z, 0-9 and `-`. Found: `invalid.bucket#`')),
    );
  });
  test('object lambda @us-east-1 [227]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('object lambda @us-west-2 [228]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('object lambda, colon resource deliminator @us-west-2 [229]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint:mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'object lambda @us-east-1, client region us-west-2, useArnRegion=true [230]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: true,
        bucket:
            'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'object lambda @us-east-1, client region s3-external-1, useArnRegion=true [231]',
      () {
    final resolved = resolveEndpoint(
        region: 's3-external-1',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: true,
        bucket:
            'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'object lambda @us-east-1, client region s3-external-1, useArnRegion=false [232]',
      () {
    expect(
      () => resolveEndpoint(
          region: 's3-external-1',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `s3-external-1` and UseArnRegion is `false`')),
    );
  });
  test(
      'object lambda @us-east-1, client region aws-global, useArnRegion=true [233]',
      () {
    final resolved = resolveEndpoint(
        region: 'aws-global',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: true,
        bucket:
            'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'object lambda @us-east-1, client region aws-global, useArnRegion=false [234]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `aws-global` and UseArnRegion is `false`')),
    );
  });
  test(
      'object lambda @cn-north-1, client region us-west-2 (cross partition), useArnRegion=true [235]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'aws-global',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws-cn:s3-object-lambda:cn-north-1:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Client was configured for partition `aws` but ARN (`arn:aws-cn:s3-object-lambda:cn-north-1:123456789012:accesspoint/mybanner`) has `aws-cn`')),
    );
  });
  test('object lambda with dualstack [236]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: true,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Object Lambda does not support Dual-stack')),
    );
  });
  test('object lambda @us-gov-east-1 [237]', () {
    final resolved = resolveEndpoint(
        region: 'us-gov-east-1',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws-us-gov:s3-object-lambda:us-gov-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda.us-gov-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-gov-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('object lambda @us-gov-east-1, with fips [238]', () {
    final resolved = resolveEndpoint(
        region: 'us-gov-east-1',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws-us-gov:s3-object-lambda:us-gov-east-1:123456789012:accesspoint/mybanner');
    expect(resolved.url,
        'https://mybanner-123456789012.s3-object-lambda-fips.us-gov-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-gov-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('object lambda @cn-north-1, with fips [239]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws-cn:s3-object-lambda:cn-north-1:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('object lambda with accelerate [240]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: true,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Object Lambda does not support S3 Accelerate')),
    );
  });
  test('object lambda with invalid arn - bad service and someresource [241]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket: 'arn:aws:sqs:us-west-2:123456789012:someresource'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: Unrecognized format: arn:aws:sqs:us-west-2:123456789012:someresource (type: someresource)')),
    );
  });
  test('object lambda with invalid arn - invalid resource [242]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:bucket_name:mybucket'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: Object Lambda ARNs only support `accesspoint` arn types, but found: `bucket_name`')),
    );
  });
  test('object lambda with invalid arn - missing region [243]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: false,
          bucket:
              'arn:aws:s3-object-lambda::123456789012:accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid ARN: bucket ARN is missing a region')),
    );
  });
  test('object lambda with invalid arn - missing account-id [244]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket: 'arn:aws:s3-object-lambda:us-west-2::accesspoint/mybanner'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Invalid ARN: Missing account id')),
    );
  });
  test(
      'object lambda with invalid arn - account id contains invalid characters [245]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123.45678.9012:accesspoint:mybucket'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The account id may only contain a-z, A-Z, 0-9 and `-`. Found: `123.45678.9012`')),
    );
  });
  test('object lambda with invalid arn - missing access point name [246]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: Expected a resource of the format `accesspoint:<accesspoint name>` but no name was provided')),
    );
  });
  test(
      'object lambda with invalid arn - access point name contains invalid character: * [247]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint:*'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The access point name may only contain a-z, A-Z, 0-9 and `-`. Found: `*`')),
    );
  });
  test(
      'object lambda with invalid arn - access point name contains invalid character: . [248]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint:my.bucket'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The access point name may only contain a-z, A-Z, 0-9 and `-`. Found: `my.bucket`')),
    );
  });
  test(
      'object lambda with invalid arn - access point name contains sub resources [249]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useArnRegion: true,
          bucket:
              'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint:mybucket:object:foo'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The ARN may only contain a single resource component after `accesspoint`.')),
    );
  });
  test('object lambda with custom endpoint [250]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useArnRegion: false,
        bucket:
            'arn:aws:s3-object-lambda:us-west-2:123456789012:accesspoint/mybanner',
        endpoint: 'https://my-endpoint.com');
    expect(resolved.url, 'https://mybanner-123456789012.my-endpoint.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('object lambda arn with region mismatch and UseArnRegion=false [251]',
      () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          bucket:
              'arn:aws:s3-object-lambda:us-east-1:123456789012:accesspoint/mybanner',
          forcePathStyle: false,
          useArnRegion: false,
          region: 'us-west-2',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid configuration: region from ARN `us-east-1` does not match client region `us-west-2` and UseArnRegion is `false`')),
    );
  });
  test('WriteGetObjectResponse @ us-west-2 [252]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        useObjectLambdaEndpoint: true,
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3-object-lambda.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('WriteGetObjectResponse with custom endpoint [253]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        useObjectLambdaEndpoint: true,
        endpoint: 'https://my-endpoint.com',
        region: 'us-west-2',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://my-endpoint.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('WriteGetObjectResponse @ us-east-1 [254]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        useObjectLambdaEndpoint: true,
        region: 'us-east-1',
        useDualStack: false,
        useFips: false);
    expect(resolved.url, 'https://s3-object-lambda.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('WriteGetObjectResponse with fips [255]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        useObjectLambdaEndpoint: true,
        region: 'us-east-1',
        useDualStack: false,
        useFips: true);
    expect(
        resolved.url, 'https://s3-object-lambda-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('WriteGetObjectResponse with dualstack [256]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          useObjectLambdaEndpoint: true,
          region: 'us-east-1',
          useDualStack: true,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Object Lambda does not support Dual-stack')),
    );
  });
  test('WriteGetObjectResponse with accelerate [257]', () {
    expect(
      () => resolveEndpoint(
          accelerate: true,
          useObjectLambdaEndpoint: true,
          region: 'us-east-1',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3 Object Lambda does not support S3 Accelerate')),
    );
  });
  test('WriteGetObjectResponse with fips in CN [258]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          region: 'cn-north-1',
          useObjectLambdaEndpoint: true,
          useDualStack: false,
          useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('WriteGetObjectResponse with invalid partition [259]', () {
    expect(
      () => resolveEndpoint(
          accelerate: false,
          useObjectLambdaEndpoint: true,
          region: 'not a valid DNS name',
          useDualStack: false,
          useFips: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid region: region was not a valid DNS name.')),
    );
  });
  test('WriteGetObjectResponse with an unknown partition [260]', () {
    final resolved = resolveEndpoint(
        accelerate: false,
        useObjectLambdaEndpoint: true,
        region: 'us-east.special',
        useDualStack: false,
        useFips: false);
    expect(
        resolved.url, 'https://s3-object-lambda.us-east.special.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3-object-lambda');
    expect(resolved.authSchemes[0].signingRegion, 'us-east.special');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Real Outpost Prod us-west-1 [261]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-1',
        bucket:
            'test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3.op-0b1d075431d83bebd.s3-outposts.us-west-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-west-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Real Outpost Prod ap-east-1 [262]', () {
    final resolved = resolveEndpoint(
        region: 'ap-east-1',
        bucket:
            'test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3.op-0b1d075431d83bebd.s3-outposts.ap-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'ap-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Ec2 Outpost Prod us-east-1 [263]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket:
            'test-accessp-e0000075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://test-accessp-e0000075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3.ec2.s3-outposts.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Ec2 Outpost Prod me-south-1 [264]', () {
    final resolved = resolveEndpoint(
        region: 'me-south-1',
        bucket:
            'test-accessp-e0000075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://test-accessp-e0000075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3.ec2.s3-outposts.me-south-1.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'me-south-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Real Outpost Beta [265]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket:
            'test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kbeta0--op-s3',
        endpoint: 'https://example.amazonaws.com',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kbeta0--op-s3.op-0b1d075431d83bebd.example.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias Ec2 Outpost Beta [266]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket:
            '161743052723-e00000136899934034jeahy1t8gpzpbwjj8kb7beta0--op-s3',
        endpoint: 'https://example.amazonaws.com',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url,
        'https://161743052723-e00000136899934034jeahy1t8gpzpbwjj8kb7beta0--op-s3.ec2.example.amazonaws.com');
    expect(resolved.authSchemes.length, 2);
    expect(resolved.authSchemes[0].name, 'sigv4a');
    expect(resolved.authSchemes[0].signingName, 's3-outposts');
    expect(resolved.authSchemes[0].signingRegion, isNull);
    expect(resolved.authSchemes[0].signingRegionSet, ['*']);
    expect(resolved.authSchemes[1].name, 'sigv4');
    expect(resolved.authSchemes[1].signingName, 's3-outposts');
    expect(resolved.authSchemes[1].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Outposts bucketAlias - No endpoint set for beta [267]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket:
              'test-accessp-o0b1d075431d83bebde8xz5w8ijx1qzlbp3i3kbeta0--op-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Expected a endpoint to be specified but no endpoint was found')),
    );
  });
  test('S3 Outposts invalid bucket name [268]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket:
              'test-accessp-o0b1de75431d83bebd/8xz5w8ijx1qzlbp3i3kbeta0--op-s3',
          endpoint: 'https://example.amazonaws.com',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Outposts Bucket alias - it must be a valid bucket name.')),
    );
  });
  test('S3 Outposts bucketAlias Invalid hardware type [269]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket:
              'test-accessp-h0000075431d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Unrecognized hardware type: "Expected hardware type o or e but got h"')),
    );
  });
  test('S3 Outposts bucketAlias Special character in Outpost Arn [270]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket:
              'test-accessp-o00000754%1d83bebde8xz5w8ijx1qzlbp3i3kuse10--op-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid ARN: The outpost Id must only contain a-z, A-Z, 0-9 and `-`.')),
    );
  });
  test('S3 Outposts bucketAlias - No endpoint set for beta [271]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket:
              'test-accessp-e0b1d075431d83bebde8xz5w8ijx1qzlbp3i3ebeta0--op-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Expected a endpoint to be specified but no endpoint was found')),
    );
  });
  test('S3 Snow with bucket [272]', () {
    final resolved = resolveEndpoint(
        region: 'snow',
        bucket: 'bucketName',
        endpoint: 'http://10.0.1.12:433',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'http://10.0.1.12:433/bucketName');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'snow');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Snow without bucket [273]', () {
    final resolved = resolveEndpoint(
        region: 'snow',
        endpoint: 'https://10.0.1.12:433',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://10.0.1.12:433');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'snow');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Snow no port [274]', () {
    final resolved = resolveEndpoint(
        region: 'snow',
        bucket: 'bucketName',
        endpoint: 'http://10.0.1.12',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'http://10.0.1.12/bucketName');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'snow');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('S3 Snow dns endpoint [275]', () {
    final resolved = resolveEndpoint(
        region: 'snow',
        bucket: 'bucketName',
        endpoint: 'https://amazonaws.com',
        useFips: false,
        useDualStack: false,
        accelerate: false);
    expect(resolved.url, 'https://amazonaws.com/bucketName');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3');
    expect(resolved.authSchemes[0].signingRegion, 'snow');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone name [276]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--abcd-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--abcd-ab1--x-s3.s3express-abcd-ab1.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone name china region [277]', () {
    final resolved = resolveEndpoint(
        region: 'cn-north-1',
        bucket: 'mybucket--abcd-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--abcd-ab1--x-s3.s3express-abcd-ab1.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone name with AP [278]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'myaccesspoint--abcd-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--abcd-ab1--xa-s3.s3express-abcd-ab1.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone name with AP china region [279]', () {
    final resolved = resolveEndpoint(
        region: 'cn-north-1',
        bucket: 'myaccesspoint--abcd-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--abcd-ab1--xa-s3.s3express-abcd-ab1.cn-north-1.amazonaws.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone names (13 chars) [280]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone names (13 chars) with AP [281]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with medium zone names (14 chars) [282]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with medium zone names (14 chars) with AP [283]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone names (20 chars) [284]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone names (20 chars) [285]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone fips [286]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test-ab1--x-s3.s3express-fips-test-ab1.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone fips china region [287]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          bucket: 'mybucket--test-ab1--x-s3',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('Data Plane with short zone fips with AP [288]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'myaccesspoint--test-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test-ab1--xa-s3.s3express-fips-test-ab1.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone fips with AP china region [289]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          bucket: 'myaccesspoint--test-ab1--xa-s3',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('Data Plane with short zone (13 chars) fips [290]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-fips-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short zone (13 chars) fips with AP [291]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-fips-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with medium zone (14 chars) fips [292]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-fips-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with medium zone (14 chars) fips with AP [293]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-fips-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone (20 chars) fips [294]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-fips-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone (20 chars) fips with AP [295]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-fips-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long AZ [296]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-az1--x-s3.s3express-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long AZ with AP [297]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-az1--xa-s3.s3express-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long AZ fips [298]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-az1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-az1--x-s3.s3express-fips-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long AZ fips with AP [299]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-az1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-az1--xa-s3.s3express-fips-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with short AZ bucket [300]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control.us-east-1.amazonaws.com/mybucket--test-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with short AZ bucket china region [301]', () {
    final resolved = resolveEndpoint(
        region: 'cn-north-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control.cn-north-1.amazonaws.com.cn/mybucket--test-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with short AZ bucket and fips [302]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control-fips.us-east-1.amazonaws.com/mybucket--test-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with short AZ bucket and fips china region [303]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1',
          bucket: 'mybucket--test-ab1--x-s3',
          useFips: true,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: true,
          disableS3ExpressSessionAuth: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message, 'message', 'Partition does not support FIPS')),
    );
  });
  test('Control plane without bucket [304]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url, 'https://s3express-control.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane without bucket and fips [305]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(
        resolved.url, 'https://s3express-control-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ [306]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-usw2-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ with AP [307]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-usw2-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short zone (13 chars) [308]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short zone (13 chars) with AP [309]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ fips [310]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-fips-usw2-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ fips with AP [311]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-fips-usw2-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short zone (13 chars) fips [312]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-fips-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short zone (13 chars) fips with AP [313]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-fips-test-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long AZ [314]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-az1--x-s3.s3express-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long AZ with AP [315]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-az1--xa-s3.s3express-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with medium zone(14 chars) [316]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with medium zone(14 chars) with AP [317]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long zone(20 chars) [318]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long zone(20 chars) with AP [319]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long AZ fips [320]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-az1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-az1--x-s3.s3express-fips-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long AZ fips with AP [321]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-az1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-az1--xa-s3.s3express-fips-test1-az1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with medium zone (14 chars) fips [322]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-fips-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with medium zone (14 chars) fips with AP [323]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-fips-test1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long zone (20 chars) fips [324]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-fips-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long zone (20 chars) fips with AP [325]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-fips-test1-long1-zone-ab1.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control Plane host override [326]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: true,
        endpoint: 'https://custom.com');
    expect(resolved.url, 'https://mybucket--usw2-az1--x-s3.custom.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control Plane host override with AP [327]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: true,
        endpoint: 'https://custom.com');
    expect(resolved.url, 'https://myaccesspoint--usw2-az1--xa-s3.custom.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control Plane host override no bucket [328]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: true,
        endpoint: 'https://custom.com');
    expect(resolved.url, 'https://custom.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane host override non virtual session auth [329]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        endpoint: 'https://10.0.0.1');
    expect(resolved.url, 'https://10.0.0.1/mybucket--usw2-az1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane host override non virtual session auth with AP [330]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        endpoint: 'https://10.0.0.1');
    expect(resolved.url, 'https://10.0.0.1/myaccesspoint--usw2-az1--xa-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control Plane host override ip [331]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: true,
        endpoint: 'https://10.0.0.1');
    expect(resolved.url, 'https://10.0.0.1/mybucket--usw2-az1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control Plane host override ip with AP [332]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: true,
        endpoint: 'https://10.0.0.1');
    expect(resolved.url, 'https://10.0.0.1/myaccesspoint--usw2-az1--xa-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane host override [333]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        endpoint: 'https://custom.com');
    expect(resolved.url, 'https://mybucket--usw2-az1--x-s3.custom.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane host override with AP [334]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        endpoint: 'https://custom.com');
    expect(resolved.url, 'https://myaccesspoint--usw2-az1--xa-s3.custom.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('bad format error [335]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'mybucket--usaz1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Unrecognized S3Express bucket name format.')),
    );
  });
  test('bad AP format error [336]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'myaccesspoint--usaz1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Unrecognized S3Express bucket name format.')),
    );
  });
  test('bad format error no session auth [337]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'mybucket--usaz1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false,
          disableS3ExpressSessionAuth: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Unrecognized S3Express bucket name format.')),
    );
  });
  test('bad AP format error no session auth [338]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'myaccesspoint--usaz1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false,
          disableS3ExpressSessionAuth: true),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Unrecognized S3Express bucket name format.')),
    );
  });
  test('accelerate error [339]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'mybucket--test-ab1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: true,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3Express does not support S3 Accelerate.')),
    );
  });
  test('accelerate error with AP [340]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'myaccesspoint--test-ab1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: true,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'S3Express does not support S3 Accelerate.')),
    );
  });
  test('Data plane bucket format error [341]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'my.bucket--test-ab1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('Data plane AP format error [342]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          bucket: 'my.myaccesspoint--test-ab1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          useS3ExpressControlEndpoint: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('host override data plane bucket error session auth [343]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          bucket: 'my.bucket--usw2-az1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          endpoint: 'https://custom.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('host override data plane AP error session auth [344]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          bucket: 'my.myaccesspoint--usw2-az1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          endpoint: 'https://custom.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('host override data plane bucket error [345]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          bucket: 'my.bucket--usw2-az1--x-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          endpoint: 'https://custom.com',
          disableS3ExpressSessionAuth: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('host override data plane AP error [346]', () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          bucket: 'my.myaccesspoint--usw2-az1--xa-s3',
          useFips: false,
          useDualStack: false,
          accelerate: false,
          endpoint: 'https://custom.com',
          disableS3ExpressSessionAuth: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'S3Express bucket name is not a valid virtual hostable name.')),
    );
  });
  test('Control plane without bucket and dualstack [347]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane without bucket, fips and dualstack [348]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control-fips.dualstack.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with bucket containing delimiters [349]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'my--s3--bucket--abcd-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://my--s3--bucket--abcd-ab1--x-s3.s3express-abcd-ab1.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with with bucket containing delimiters [350]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'my--s3--bucket--abcd-ab1--x-s3',
        useFips: false,
        useDualStack: false,
        accelerate: false,
        useS3ExpressControlEndpoint: true,
        disableS3ExpressSessionAuth: false);
    expect(resolved.url,
        'https://s3express-control.us-east-1.amazonaws.com/my--s3--bucket--abcd-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short AZ and dualstack [351]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with short AZ and FIPS with dualstack [352]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-fips-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ and dualstack [353]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with short AZ and FIPS with dualstack [354]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az1--x-s3.s3express-fips-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone and dualstack [355]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az12--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--usw2-az12--x-s3.s3express-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone and FIPS with dualstack [356]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az12--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--usw2-az12--x-s3.s3express-fips-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with zone and dualstack [357]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az12--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az12--x-s3.s3express-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with 9-char zone and FIPS with dualstack [358]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--usw2-az12--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--usw2-az12--x-s3.s3express-fips-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with 13-char zone and dualstack [359]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with 13-char zone and FIPS with dualstack [360]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-fips-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with 13-char zone and dualstack [361]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with 13-char zone and FIPS with dualstack [362]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test-zone-ab1--x-s3.s3express-fips-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with 14-char zone and dualstack [363]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with 14-char zone and FIPS with dualstack [364]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-fips-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with 14-char zone and dualstack [365]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with 14-char zone and FIPS with dualstack [366]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-zone-ab1--x-s3.s3express-fips-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone (20 cha) and dualstack [367]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with long zone (20 char) and FIPS with dualstack [368]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-fips-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with long zone (20 char) and dualstack [369]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Data Plane sigv4 auth with long zone (20 char) and FIPS with dualstack [370]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'mybucket--test1-long1-zone-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://mybucket--test1-long1-zone-ab1--x-s3.s3express-fips-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane and FIPS with dualstack [371]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: true);
    expect(resolved.url,
        'https://s3express-control-fips.dualstack.us-east-1.amazonaws.com/mybucket--test-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane with zone and dualstack and AP [372]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane with zone and FIPS with dualstack and AP [373]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-fips-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with zone and dualstack and AP [374]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane AP sigv4 auth with zone and FIPS with dualstack [375]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az1--xa-s3.s3express-fips-usw2-az1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone (9 char) and AP with dualstack [376]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az12--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az12--xa-s3.s3express-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone (9 char) and FIPS with AP and dualstack [377]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az12--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az12--xa-s3.s3express-fips-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with (9 char) zone and dualstack with AP [378]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az12--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az12--xa-s3.s3express-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Access Point sigv4 auth with (9 char) zone and FIPS with dualstack [379]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--usw2-az12--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--usw2-az12--xa-s3.s3express-fips-usw2-az12.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone (13 char) and AP with dualstack [380]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with zone (13 char) and AP with FIPS and dualstack [381]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-fips-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with (13 char) zone with AP and dualstack [382]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Data Plane sigv4 auth with (13 char) zone with AP and FIPS and dualstack [383]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test-zone-ab1--xa-s3.s3express-fips-test-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with (14 char) zone and AP with dualstack [384]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with (14 char) zone and AP with FIPS and dualstack [385]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-fips-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane sigv4 auth with (14 char) zone and AP with dualstack [386]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with (14 char) zone and AP with FIPS and dualstack [387]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-zone-ab1--xa-s3.s3express-fips-test1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with (20 char) zone and AP with dualstack [388]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data Plane with (20 char) zone and AP with FIPS and dualstack [389]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: false);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-fips-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4-s3express');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane AP with sigv4 and dualstack [390]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Data plane AP sigv4 with fips and dualstack [391]', () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        bucket: 'myaccesspoint--test1-long1-zone-ab1--xa-s3',
        useFips: true,
        useDualStack: true,
        accelerate: false,
        disableS3ExpressSessionAuth: true);
    expect(resolved.url,
        'https://myaccesspoint--test1-long1-zone-ab1--xa-s3.s3express-fips-test1-long1-zone-ab1.dualstack.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-west-2');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane with dualstack and bucket [392]', () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        bucket: 'mybucket--test-ab1--x-s3',
        useFips: false,
        useDualStack: true,
        accelerate: false,
        useS3ExpressControlEndpoint: true);
    expect(resolved.url,
        'https://s3express-control.dualstack.us-east-1.amazonaws.com/mybucket--test-ab1--x-s3');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 's3express');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
}
