// AUTO-GENERATED: This file is generated by the `generator` package.
//
// Endpoint resolution conformance tests, derived from the service's
// `smithy.rules#endpointTests` trait.

// ignore_for_file: prefer_single_quotes, prefer_collection_literals, unnecessary_lambdas, unused_import

import 'package:aws_client/src/generated/code_catalyst/v2022_09_28.endpoints.dart';
import 'package:aws_client/src/shared/src/protocol/endpoints/endpoint_resolver.dart';
import 'package:test/test.dart';

void main() {
  test('Override endpoint [0]', () {
    final resolved =
        resolveEndpoint(endpoint: 'https://test.codecatalyst.global.api.aws');
    expect(resolved.url, 'https://test.codecatalyst.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default endpoint (region not set) [1]', () {
    final resolved = resolveEndpoint();
    expect(resolved.url, 'https://codecatalyst.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default FIPS endpoint (region not set) [2]', () {
    final resolved = resolveEndpoint(useFips: true);
    expect(resolved.url, 'https://codecatalyst-fips.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default endpoint (region: aws-global) [3]', () {
    final resolved = resolveEndpoint(region: 'aws-global');
    expect(resolved.url, 'https://codecatalyst.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default FIPS endpoint (region: aws-global) [4]', () {
    final resolved = resolveEndpoint(region: 'aws-global', useFips: true);
    expect(resolved.url, 'https://codecatalyst-fips.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default endpoint for a valid home region (region: us-west-2) [5]', () {
    final resolved = resolveEndpoint(region: 'us-west-2');
    expect(resolved.url, 'https://codecatalyst.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Default FIPS endpoint for a valid home region (region: us-west-2) [6]',
      () {
    final resolved = resolveEndpoint(region: 'us-west-2', useFips: true);
    expect(resolved.url, 'https://codecatalyst-fips.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Default endpoint for an unavailable home region (region: us-east-1) [7]',
      () {
    final resolved = resolveEndpoint(region: 'us-east-1');
    expect(resolved.url, 'https://codecatalyst.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Default FIPS endpoint for an unavailable home region (region: us-east-1) [8]',
      () {
    final resolved = resolveEndpoint(region: 'us-east-1', useFips: true);
    expect(resolved.url, 'https://codecatalyst-fips.global.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
}
