// AUTO-GENERATED: This file is generated by the `generator` package.
//
// Endpoint resolution conformance tests, derived from the service's
// `smithy.rules#endpointTests` trait.

// ignore_for_file: prefer_single_quotes, prefer_collection_literals, unnecessary_lambdas, unused_import

import 'package:aws_client/src/generated/arc_region_switch/v2022_07_26.endpoints.dart';
import 'package:aws_client/src/shared/src/protocol/endpoints/endpoint_resolver.dart';
import 'package:test/test.dart';

void main() {
  test('For custom endpoint with region not set and fips disabled [0]', () {
    final resolved =
        resolveEndpoint(endpoint: 'https://example.com', useFips: false);
    expect(resolved.url, 'https://example.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For custom endpoint with fips enabled [1]', () {
    expect(
      () => resolveEndpoint(endpoint: 'https://example.com', useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS and custom endpoint are not supported')),
    );
  });
  test('For region us-east-1 with FIPS enabled and DualStack enabled [2]', () {
    final resolved = resolveEndpoint(region: 'us-east-1', useFips: true);
    expect(resolved.url, 'https://arc-region-switch-fips.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For region us-east-1 with FIPS disabled and DualStack enabled [3]', () {
    final resolved = resolveEndpoint(region: 'us-east-1', useFips: false);
    expect(resolved.url, 'https://arc-region-switch.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For region cn-northwest-1 with FIPS enabled and DualStack enabled [4]',
      () {
    final resolved = resolveEndpoint(region: 'cn-northwest-1', useFips: true);
    expect(resolved.url,
        'https://arc-region-switch-fips.cn-northwest-1.api.amazonwebservices.com.cn');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For region cn-northwest-1 with FIPS disabled and DualStack enabled [5]',
      () {
    final resolved = resolveEndpoint(region: 'cn-northwest-1', useFips: false);
    expect(resolved.url,
        'https://arc-region-switch.cn-northwest-1.api.amazonwebservices.com.cn');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For region us-gov-west-1 with FIPS enabled and DualStack enabled [6]',
      () {
    final resolved = resolveEndpoint(region: 'us-gov-west-1', useFips: true);
    expect(
        resolved.url, 'https://arc-region-switch-fips.us-gov-west-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('For region us-gov-west-1 with FIPS disabled and DualStack enabled [7]',
      () {
    final resolved = resolveEndpoint(region: 'us-gov-west-1', useFips: false);
    expect(resolved.url, 'https://arc-region-switch.us-gov-west-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Missing region [8]', () {
    expect(
      () => resolveEndpoint(),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid Configuration: Missing Region')),
    );
  });
  test(
      'Control plane operation with DualStack in us-west-2 routes to us-east-1 DualStack endpoint [9]',
      () {
    final resolved =
        resolveEndpoint(region: 'us-west-2', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation with endpoint set in us-east-1 routes to provided endpoint [10]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1',
        useControlPlaneEndpoint: true,
        endpoint: 'https://amazonaws.com');
    expect(resolved.url, 'https://amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation with endpoint set in us-west-2 routes to provided endpoint [11]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2',
        useControlPlaneEndpoint: true,
        endpoint: 'https://amazonaws.com');
    expect(resolved.url, 'https://amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation in us-west-2 (standard partition) routes to us-east-1 [12]',
      () {
    final resolved =
        resolveEndpoint(region: 'us-west-2', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation in cn-north-1 (China partition) routes to cn-north-1 with China DNS suffix [13]',
      () {
    final resolved =
        resolveEndpoint(region: 'cn-north-1', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.cn-north-1.api.amazonwebservices.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation in cn-northwest-1 (China partition) routes to cn-north-1 with China DNS suffix [14]',
      () {
    final resolved = resolveEndpoint(
        region: 'cn-northwest-1', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.cn-north-1.api.amazonwebservices.com.cn');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'cn-north-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation in us-gov-west-1 (GovCloud partition) routes to us-gov-west-1 with GovCloud DNS suffix [15]',
      () {
    final resolved =
        resolveEndpoint(region: 'us-gov-west-1', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.us-gov-west-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-gov-west-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation in us-gov-east-1 (GovCloud partition) routes to us-gov-west-1 with GovCloud DNS suffix [16]',
      () {
    final resolved =
        resolveEndpoint(region: 'us-gov-east-1', useControlPlaneEndpoint: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane.us-gov-west-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-gov-west-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation with FIPS in us-west-2 routes to us-east-1 FIPS endpoint [17]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-west-2', useControlPlaneEndpoint: true, useFips: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane-fips.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Control plane operation with FIPS in us-east-1 routes to us-east-1 FIPS endpoint [18]',
      () {
    final resolved = resolveEndpoint(
        region: 'us-east-1', useControlPlaneEndpoint: true, useFips: true);
    expect(resolved.url,
        'https://arc-region-switch-control-plane-fips.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 1);
    expect(resolved.authSchemes[0].name, 'sigv4');
    expect(resolved.authSchemes[0].signingName, 'arc-region-switch');
    expect(resolved.authSchemes[0].signingRegion, 'us-east-1');
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Control plane operation with FIPS in CN returns an error [19]', () {
    expect(
      () => resolveEndpoint(
          region: 'cn-north-1', useControlPlaneEndpoint: true, useFips: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS is not supported in this partition')),
    );
  });
  test(
      'Control plane operation with endpoint set using FIPS in us-east-1 errors [20]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-east-1',
          useControlPlaneEndpoint: true,
          useFips: true,
          endpoint: 'https://amazonaws.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS and custom endpoint are not supported')),
    );
  });
  test(
      'Control plane operation with endpoint set using FIPS in us-west-2 routes to provided endpoint [21]',
      () {
    expect(
      () => resolveEndpoint(
          region: 'us-west-2',
          useControlPlaneEndpoint: true,
          useFips: true,
          endpoint: 'https://amazonaws.com'),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS and custom endpoint are not supported')),
    );
  });
}
