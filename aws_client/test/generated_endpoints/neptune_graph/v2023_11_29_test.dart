// AUTO-GENERATED: This file is generated by the `generator` package.
//
// Endpoint resolution conformance tests, derived from the service's
// `smithy.rules#endpointTests` trait.

// ignore_for_file: prefer_single_quotes, prefer_collection_literals, unnecessary_lambdas, unused_import

import 'package:aws_client/src/generated/neptune_graph/v2023_11_29.endpoints.dart';
import 'package:aws_client/src/shared/src/protocol/endpoints/endpoint_resolver.dart';
import 'package:test/test.dart';

void main() {
  test('Region and CP ApiType are provided [0]', () {
    final resolved = resolveEndpoint(
        endpoint: 'https://mycustomDomain.com',
        apiType: 'ControlPlane',
        region: 'us-east-1');
    expect(resolved.url, 'https://mycustomDomain.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Region and DP ApiType are provided [1]', () {
    final resolved = resolveEndpoint(
        endpoint: 'https://mycustomDomain.com',
        apiType: 'DataPlane',
        region: 'us-east-1');
    expect(resolved.url, 'https://mycustomDomain.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Region and invalid ApiType are provided [2]', () {
    final resolved = resolveEndpoint(
        endpoint: 'https://mycustomDomain.com',
        apiType: 'someInvalidApiType',
        region: 'us-east-1');
    expect(resolved.url, 'https://mycustomDomain.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Only invalid ApiType is provided [3]', () {
    final resolved = resolveEndpoint(
        endpoint: 'https://mycustomDomain.com', apiType: 'someInvalidApiType');
    expect(resolved.url, 'https://mycustomDomain.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-east-1, useFipsEndpoint: true, useDualStackEndpoint: true [4]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-1',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-1, useFipsEndpoint: true, useDualStackEndpoint: true [5]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-east-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-east-1, useFipsEndpoint: true, useDualStackEndpoint: false [6]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-1',
        useFips: true,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph-fips.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-1, useFipsEndpoint: true, useDualStackEndpoint: false [7]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-east-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-east-1, useFipsEndpoint: false, useDualStackEndpoint: true [8]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-east-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-1, useFipsEndpoint: false, useDualStackEndpoint: true [9]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-east-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-east-1.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-east-1, useFipsEndpoint: false, useDualStackEndpoint: false [10]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.us-east-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-1, useFipsEndpoint: false, useDualStackEndpoint: false [11]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-east-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://us-east-1.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-east-2, useFipsEndpoint: true, useDualStackEndpoint: true [12]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-2',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.us-east-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-2, useFipsEndpoint: true, useDualStackEndpoint: true [13]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-east-2',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-east-2, useFipsEndpoint: true, useDualStackEndpoint: false [14]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-2',
        useFips: true,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph-fips.us-east-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-2, useFipsEndpoint: true, useDualStackEndpoint: false [15]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-east-2',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-east-2, useFipsEndpoint: false, useDualStackEndpoint: true [16]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-east-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-2, useFipsEndpoint: false, useDualStackEndpoint: true [17]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-east-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-east-2.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-east-2, useFipsEndpoint: false, useDualStackEndpoint: false [18]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-east-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.us-east-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-east-2, useFipsEndpoint: false, useDualStackEndpoint: false [19]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-east-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://us-east-2.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-west-2, useFipsEndpoint: true, useDualStackEndpoint: true [20]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-west-2',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.us-west-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-west-2, useFipsEndpoint: true, useDualStackEndpoint: true [21]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-west-2',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-west-2, useFipsEndpoint: true, useDualStackEndpoint: false [22]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-west-2',
        useFips: true,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph-fips.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-west-2, useFipsEndpoint: true, useDualStackEndpoint: false [23]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'us-west-2',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: us-west-2, useFipsEndpoint: false, useDualStackEndpoint: true [24]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-west-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-west-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-west-2, useFipsEndpoint: false, useDualStackEndpoint: true [25]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-west-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.us-west-2.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: us-west-2, useFipsEndpoint: false, useDualStackEndpoint: false [26]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'us-west-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.us-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: us-west-2, useFipsEndpoint: false, useDualStackEndpoint: false [27]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'us-west-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://us-west-2.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-west-1, useFipsEndpoint: true, useDualStackEndpoint: true [28]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-1',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.eu-west-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-1, useFipsEndpoint: true, useDualStackEndpoint: true [29]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-west-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-west-1, useFipsEndpoint: true, useDualStackEndpoint: false [30]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-1',
        useFips: true,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph-fips.eu-west-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-1, useFipsEndpoint: true, useDualStackEndpoint: false [31]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-west-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-west-1, useFipsEndpoint: false, useDualStackEndpoint: true [32]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-west-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-1, useFipsEndpoint: false, useDualStackEndpoint: true [33]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-west-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-west-1.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-west-1, useFipsEndpoint: false, useDualStackEndpoint: false [34]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.eu-west-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-1, useFipsEndpoint: false, useDualStackEndpoint: false [35]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-west-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://eu-west-1.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-west-2, useFipsEndpoint: true, useDualStackEndpoint: true [36]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-2',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.eu-west-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-2, useFipsEndpoint: true, useDualStackEndpoint: true [37]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-west-2',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-west-2, useFipsEndpoint: true, useDualStackEndpoint: false [38]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-2',
        useFips: true,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph-fips.eu-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-2, useFipsEndpoint: true, useDualStackEndpoint: false [39]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-west-2',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-west-2, useFipsEndpoint: false, useDualStackEndpoint: true [40]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-west-2.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-2, useFipsEndpoint: false, useDualStackEndpoint: true [41]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-west-2',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-west-2.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-west-2, useFipsEndpoint: false, useDualStackEndpoint: false [42]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-west-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.eu-west-2.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-west-2, useFipsEndpoint: false, useDualStackEndpoint: false [43]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-west-2',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://eu-west-2.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-central-1, useFipsEndpoint: true, useDualStackEndpoint: true [44]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-central-1',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.eu-central-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-central-1, useFipsEndpoint: true, useDualStackEndpoint: true [45]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-central-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-central-1, useFipsEndpoint: true, useDualStackEndpoint: false [46]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-central-1',
        useFips: true,
        useDualStack: false);
    expect(
        resolved.url, 'https://neptune-graph-fips.eu-central-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-central-1, useFipsEndpoint: true, useDualStackEndpoint: false [47]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'eu-central-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: eu-central-1, useFipsEndpoint: false, useDualStackEndpoint: true [48]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-central-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-central-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-central-1, useFipsEndpoint: false, useDualStackEndpoint: true [49]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-central-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.eu-central-1.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: eu-central-1, useFipsEndpoint: false, useDualStackEndpoint: false [50]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'eu-central-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.eu-central-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: eu-central-1, useFipsEndpoint: false, useDualStackEndpoint: false [51]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'eu-central-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://eu-central-1.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: ap-southeast-1, useFipsEndpoint: true, useDualStackEndpoint: true [52]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-southeast-1',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.ap-southeast-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-southeast-1, useFipsEndpoint: true, useDualStackEndpoint: true [53]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'ap-southeast-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: ap-southeast-1, useFipsEndpoint: true, useDualStackEndpoint: false [54]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-southeast-1',
        useFips: true,
        useDualStack: false);
    expect(resolved.url,
        'https://neptune-graph-fips.ap-southeast-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-southeast-1, useFipsEndpoint: true, useDualStackEndpoint: false [55]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'ap-southeast-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: ap-southeast-1, useFipsEndpoint: false, useDualStackEndpoint: true [56]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-southeast-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.ap-southeast-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-southeast-1, useFipsEndpoint: false, useDualStackEndpoint: true [57]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'ap-southeast-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.ap-southeast-1.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: ap-southeast-1, useFipsEndpoint: false, useDualStackEndpoint: false [58]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-southeast-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.ap-southeast-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-southeast-1, useFipsEndpoint: false, useDualStackEndpoint: false [59]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'ap-southeast-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://ap-southeast-1.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: ap-northeast-1, useFipsEndpoint: true, useDualStackEndpoint: true [60]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-northeast-1',
        useFips: true,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph-fips.ap-northeast-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-northeast-1, useFipsEndpoint: true, useDualStackEndpoint: true [61]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'ap-northeast-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: ap-northeast-1, useFipsEndpoint: true, useDualStackEndpoint: false [62]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-northeast-1',
        useFips: true,
        useDualStack: false);
    expect(resolved.url,
        'https://neptune-graph-fips.ap-northeast-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-northeast-1, useFipsEndpoint: true, useDualStackEndpoint: false [63]',
      () {
    expect(
      () => resolveEndpoint(
          apiType: 'DataPlane',
          region: 'ap-northeast-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: fips endpoint is not supported for this API')),
    );
  });
  test(
      'Validate CP endpoint in region: ap-northeast-1, useFipsEndpoint: false, useDualStackEndpoint: true [64]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-northeast-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.ap-northeast-1.api.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-northeast-1, useFipsEndpoint: false, useDualStackEndpoint: true [65]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'ap-northeast-1',
        useFips: false,
        useDualStack: true);
    expect(resolved.url, 'https://neptune-graph.ap-northeast-1.on.aws');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate CP endpoint in region: ap-northeast-1, useFipsEndpoint: false, useDualStackEndpoint: false [66]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'ControlPlane',
        region: 'ap-northeast-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://neptune-graph.ap-northeast-1.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test(
      'Validate DP endpoint in region: ap-northeast-1, useFipsEndpoint: false, useDualStackEndpoint: false [67]',
      () {
    final resolved = resolveEndpoint(
        apiType: 'DataPlane',
        region: 'ap-northeast-1',
        useFips: false,
        useDualStack: false);
    expect(resolved.url, 'https://ap-northeast-1.neptune-graph.amazonaws.com');
    expect(resolved.authSchemes.length, 0);
    expect(resolved.headers.keys.toSet(), [].toSet());
  });
  test('Validate When no region is provided [68]', () {
    expect(
      () => resolveEndpoint(apiType: 'DataPlane'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid Configuration: Missing Region')),
    );
  });
  test('Validate When invalid/unknown ApiType provided [69]', () {
    expect(
      () => resolveEndpoint(region: 'us-east-1', apiType: 'someUnknownValue'),
      throwsA(isA<EndpointResolutionException>().having((e) => e.message,
          'message', 'Invalid Configuration: Unknown ApiType')),
    );
  });
  test('Both Fips and dualstack enabled [70]', () {
    expect(
      () => resolveEndpoint(
          endpoint: 'https://mycustomDomain.com',
          apiType: 'ControlPlane',
          region: 'us-east-1',
          useFips: true,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS and custom endpoint are not supported')),
    );
  });
  test('Fips enabled and dualstack disabled [71]', () {
    expect(
      () => resolveEndpoint(
          endpoint: 'https://mycustomDomain.com',
          apiType: 'ControlPlane',
          region: 'us-east-1',
          useFips: true,
          useDualStack: false),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: FIPS and custom endpoint are not supported')),
    );
  });
  test('Fips disabled and dualstack enabled [72]', () {
    expect(
      () => resolveEndpoint(
          endpoint: 'https://mycustomDomain.com',
          apiType: 'ControlPlane',
          region: 'us-east-1',
          useFips: false,
          useDualStack: true),
      throwsA(isA<EndpointResolutionException>().having(
          (e) => e.message,
          'message',
          'Invalid Configuration: Dualstack and custom endpoint are not supported')),
    );
  });
}
