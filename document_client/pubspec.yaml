name: document_client
description: An abstraction for simplifying DynamoDB operations
version: 0.0.1

homepage: https://github.com/agilord/aws_client

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  aws_dynamodb_api: ^0.0.2
  shared_aws_api: ^0.1.7

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.5
