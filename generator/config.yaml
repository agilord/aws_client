protocols:
  ec2:
    shared: 0.3.0-dev
  json:
    shared: 0.3.0-dev
  query:
    shared: 0.3.0-dev
    # publish: true
  rest-json:
    shared: 0.3.0-dev
  rest-xml:
    shared: 0.3.0-dev
    # publish: true

# Packages to generate
packages:
  - aws_lambda_api # rest-json
  - aws_dynamodb_api # json
  - aws_sns_api # query
  - aws_route53_api # rest-xml
  - aws_ec2_api # ec2