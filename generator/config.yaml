protocols:
  ec2:
    shared: ^0.1.0
  json:
    shared: ^0.1.0
  query:
    shared: ^0.1.0
    publish: true
  rest-json:
    shared: ^0.1.0
  rest-xml:
    shared: ^0.1.0
    publish: true

# Packages to generate
packages:
  - aws_lambda_api # rest-json
  - aws_dynamodb_api # json
  - aws_sns_api # query
  - aws_route53_api # rest-xml
  - aws_ec2_api # ec2